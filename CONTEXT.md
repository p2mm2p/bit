# bit

两个交互命令的 git 包装（`bit branch` / `bit commit`）：bit 只负责「合规」——分支名照 Conventional Branch、提交消息照 Conventional Commits——执行与 git 自身的语义留给 git 本体。

## 术语

### 命令面

**命令面**：
bit 自己定义「怎么被调用」的全部内容——参数、帮助、诊断、退出码；不与 git 的选项解析器对齐。
_Avoid_: 命令行契约、CLI 接口

**用法层失败**：
调用方式本身不对、bit 还没开始做事的那类失败——无参数、未知命令、未知选项、多余参数；退出码 2，诊断走 stderr。
_Avoid_: 参数错误、解析失败

**运行期失败**：
bit 开始做事之后失败或被取消——Esc 取消、非 TTY、无暂存内容、编辑器非 0 退出；退出码 1。
_Avoid_: 运行时错误、执行失败

**原样透传**：
git 的 stdout / stderr 与退出码不经翻译、不经包装地交给调用方；git 自身的失败（重名、非法 ref、非仓库）只以这种形态出现。
_Avoid_: 转发、二次包装

**命令透传**：
把调用直接转给 git 本体（`bit add .` ≡ `git add .`）；bit 明确不做，其余 git 用法请直接用 git。
_Avoid_: 代理、桥接

### 交互

**静默规范化**：
分支描述输入在进入校验前的无提示改写——trim → 全小写 → 空格/下划线转 `-` → 折叠连续 `-`/`.` → 去首尾分隔符；改写只要发生，创建前确认就回显原文。
_Avoid_: 静默纠正、清洗

**前缀剥离**：
描述输入里带了类型前缀时，同类型前缀静默剥掉、异类型前缀原地报错重输。

**创建前确认**：
`bit branch` 调用 git 之前的一次是/否确认（默认「是」）；选「否」回到名称输入并预填原文。

**未改动退出**：
编辑器退出码 0，而「git 将保存的消息」与上一轮逐字相同——bit 判为用户没打算写，按取消收场（`已取消：`、退出码 1、不提交）。改动过才带着原文重开编辑器，回环因此有界。
_Avoid_: 空提交取消、空描述回环

### 类型

**分支类型**：
`bit branch` 菜单内置的 7 个前缀：feature、fix、hotfix、release、chore、docs、test。

**提交类型**：
`bit commit` 菜单内置的 11 个类型——commitlint `type-enum` 全集。

**扩展类型**：
按 Conventional Branch「允许自定义类型」条款采纳、超出其规范正则的分支类型（docs、test）；菜单里标注为扩展。

### AI 辅助

**AI 供给**：
bit 调用大模型所需的本机配置（提供商、端点、模型、密钥），由 `bit login` 写入。没配置时「描述翻译」与「提交消息生成」不存在。
_Avoid_: LLM 后端、模型配置

**描述翻译**：
`bit branch` 的描述段在规范化与前缀剥离之后仍含非 ASCII 字符时，交给「AI 供给」译为英文，再回到 bit 自己的校验管线；只产出描述，最终名仍由 bit 组装。没配置「AI 供给」时这一步不存在，行为与 v0.1 相同。
_Avoid_: 自动翻译、机器翻译

**译文清理**：
描述翻译对模型返回文本的专用改写——取首个非空行、把标点映射为分隔符，随后仍走静默规范化；清理后为空或仍含非 ASCII 字母类字符（残留中文、emoji）即判「翻译失败」。只作用于模型返回文本，不改用户输入。
_Avoid_: 译文规范化、清洗

**翻译失败**：
描述翻译整体不可用——供给调用出错（认证 / 限流 / 网络 / 服务端 / 参数）或译文不可用（空、仍含非 ASCII）。去向是回到名称输入、预填原文；不自动重试、不静默降级。
_Avoid_: AI 错误、翻译出错

**提交消息生成**：
`bit commit --gen` 的 AI 能力——读暂存 diff 产出提交消息字段（type / scope / breaking / subject / body）的 draft，由 bit 组装、复核，确认后提交。没配置「AI 供给」时不存在。
_Avoid_: 自动提交、AI 写提交
